def print_todo(todo):
    """
    Accepts a dict and prints it with a format string

    >>> todo = {'name': 'Example Name', 'body': 'Example Body', 'points': '1'}
    >>> print_todo(todo)
    Example Name: Example Body
    >>>
    """
    pass
    print(f"{todo['name']}: {todo['body']}")
